# Postgres (docker compose default). The worker owns ONLY the `enrichment` schema
# (architecture spec: "Two schema domains") and reads settlements from the
# indexer-written schema.
DATABASE_URL=postgresql://observatory:observatory@localhost:5439/observatory

# Where Ponder writes settlements (local dev: `public`; prod: the stable views schema).
SETTLEMENTS_TABLE=public.settlements
